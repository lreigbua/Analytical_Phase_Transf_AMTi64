[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "Analytical_Phase_Transf_AMTi64"
version = "0.1.0"
requires-python = ">=3.9"

# Packages live in src/
[tool.setuptools.packages.find]
where = ["src"]
